# Project Status & CADP Anchors

## Active State
- **Status:** Complete.
- **Milestone:** Path configurability refactoring & multi-PC migration guide.
- **Verified Components:**
  - `proxy_core/config.py`: dynamic path resolution (`get_keys_location`, `get_vpn_dir`, `get_opencode_config_path`) with schema upgrade.
  - `proxy_core/server.py`: dynamic `vpn_dir` resolution with guarded `sys.path.insert`.
  - `proxy_core/gui.py`: dynamic `vpn_dir` & PowerShell script paths, Settings tab UI entries for VPN Switcher Directory and OpenCode Config Path with native Browse dialogs.
  - `tools/analyze_duplicates.py`: relative project path resolution.
  - `test_config_keys.py`: unit tests for path resolvers and schema upgrade (all 7 passing; 37 passing in `test_compactor.py`).
  - `MIGRATION_GUIDE.md`: comprehensive migration documentation covering prerequisites, path settings, API key layouts, and detailed WireGuard VPN multi-channel configuration.

## Next Actionable Step
- Ready for deployment or migration to another PC. Launch via `launch_gui.cmd`.
